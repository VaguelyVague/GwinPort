# GwinPort
A local airline booking system using JavaScript and Java for its code

# GwinPort Backend (Spring Boot)

## Setup Instructions

1. Go to backend folder:
   cd backend

2. Install dependencies:
   mvn clean install

3. Run the app:
   mvn spring-boot:run

## API Test

http://localhost:8080/api/admin/flights

